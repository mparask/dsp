% Example 8.17

b = 1;
a = poly([-0.4, 0.4, 0.4]);
[R,p,C] = residuez(b,a);

n = [0:10];
delta = zeros(1, length(n)); 
delta(1, n==0) = 1;

% x1: Κρουστική απόκριση συστήματος με επίλυση ΓΕΔΣΣ
x1 = filter(b, a, delta)

% x2: Επίλυση με αντίστροφο μετασχηματισμό Ζ
% x[n] = [0.75(0.4)^n + 0.5n(0.4)^n + 0.25(-0.4)^n] u[n]

x2 = (0.75)*(0.4).^n + (0.5)*n.*(0.4).^n + (0.25)*(-0.4).^n
